#foreign from(ctype, isdigit : (character: int) -> int); 
#foreign from(ctype, isalpha : (character: int) -> int); 

true : int = 1;
false : int = 0;

begin : () {
    i : int = isdigit('1');
    j : int = isalpha('c');
}

begin();

` 